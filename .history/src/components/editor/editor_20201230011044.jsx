import React from 'react';
import styles from "./editor.module.css";

const Editor = (props) => {
    return(
        <section styles={styles.section}>Editor</section>
    );
}
            
    
export default Editor;